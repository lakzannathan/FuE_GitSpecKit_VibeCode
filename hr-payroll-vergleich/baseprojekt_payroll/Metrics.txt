# Alles in einem Rutsch sichern:
radon cc ./src -a > payroll_base_cc_results.txt   # Komplexität (Struktur)
radon mi ./src -s > payroll_base_mi_results.txt   # Wartbarkeit (Gesamtnote)
radon hal ./src   > payroll_base_hal_results.txt  # Aufwand (Lesbarkeit)
